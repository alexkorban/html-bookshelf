# Use one rendering core for static and browser use

The package will expose a rendering function, a browser mounting helper, and a
command-line tool. Each will use the same rendering core so that a Book Entry
has the same HTML and CSS result in static and dynamic use.
