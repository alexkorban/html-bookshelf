# Keep the version-1 options interface small

Version 1 exposes only theme, layout, shelf style, variation seed, an optional
font cycle, three sizing settings, style delivery, animation, and an accessible
label. Spine text direction remains an automatic visual rule rather than a
caller-controlled option, keeping the Bookshelf interface small while the
renderer owns its responsive presentation.
